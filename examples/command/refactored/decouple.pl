java_import(['command.refactored', 
			'command.refactored.Action1',
			'command.refactored.Action2']).

hideSetFrom(action, invoker).

declareSet(action, ['clicked__void']).

declareSet(invoker, ['Button']).
			
